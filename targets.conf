# ---------------------------------------------------------------------------
# targets.conf - welche Hardware gebaut wird
#
# Wird von build.sh als zweiter Parameter eingelesen:
#   ./build.sh build.conf targets.conf domains.conf
#
# Die Reihenfolge dieser Liste ist zugleich die Baureihenfolge.
#
# Ein Target wird deaktiviert, indem ihm ein "-" vorangestellt wird. Es bleibt
# damit an seiner Position sichtbar, statt auskommentiert zu werden, und laesst
# sich durch Entfernen des einen Zeichens wieder aktivieren. Hinter dem Target
# kann in derselben Zeile ein Kommentar stehen, der den Grund festhaelt.
#
# Werden build.sh nach den drei Konfigurationsdateien noch Targets uebergeben
# ("build.sh build.conf targets.conf domains.conf ath79-generic"), hat das
# Vorrang vor dieser Liste. Das ist fuer einzelne Testbuilds gedacht - der
# regulaere Build-Umfang wird hier gepflegt.
#
# Die Zahlen in den Kommentaren sind Knoten im Feld, "bei uns / bei anderen",
# erhoben am 13.09.2026 aus der ffmuc federated map und dem Gluon-Census
# Darmstadt. Herleitung und Diskussion: router-werkstatt, docs/target-nutzung.md.
# "Pflicht" heisst: Knoten von uns laufen darauf, ohne das Target bekommen sie
# kein stable-Image.
# ---------------------------------------------------------------------------

GLUON_TARGETS=(
  -armsr-armv7          # 0 / 0
  -armsr-armv8          # 0 / 0
  ath79-generic         # 459 / 14704
  ath79-nand            # 0 / 145
  ath79-mikrotik        # 0 / 8
  -bcm27xx-bcm2708      # 0 / 5     Raspberry Pi 1
  -bcm27xx-bcm2709      # 0 / 1     Raspberry Pi 2
  ipq40xx-generic       # 73 / 1898
  ipq40xx-mikrotik      # 0 / 14
  ipq806x-generic       # 0 / 26
  lantiq-xrx200         # 7 / 478   Pflicht (FRITZ!Box 7360/7362/7412)
  lantiq-xway           # 0 / 31
  mediatek-filogic      # 58 / 343
  mediatek-mt7622       # 1 / 56
  mpc85xx-p1010         # 4 / 164   Pflicht (TL-WDR4900 v1)
  mpc85xx-p1020         # 5 / 167
  ramips-mt7620         # 3 / 64    Pflicht (Archer C20i)
  ramips-mt7621         # 191 / 2977
  ramips-mt76x8         # 21 / 1328 Pflicht (19x Netgear R6120)
  -realtek-rtl838x      # 0 / 0
  rockchip-armv8        # 0 / 76
  -sunxi-cortexa7       # 0 / 0
  x86-generic           # 3 / >=10  Pflicht; bei anderen unscharf (Karte kennt nur das Modell), neue Installationen besser x86-64
  -x86-geode            # 0 / 0
  -x86-legacy           # 0 / 0
  x86-64                # 36 / 191
  bcm27xx-bcm2710       # 0 / 16    Raspberry Pi 3
  bcm27xx-bcm2711       # 0 / 15    Raspberry Pi 4
  -mvebu-cortexa9       # 0 / 1
  -ipq40xx-chromium     # 0 / 3     Target nur durch eigenen Patch
  -ipq807x-generic      # 0 / 3     Target nur durch eigenen Patch
)
